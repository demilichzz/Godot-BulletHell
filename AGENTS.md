# 项目开发约定

## Boss 图片资源

- 后续生成的 Boss 图片默认使用真正的透明背景（PNG Alpha 通道），不使用棋盘格或纯色背景代替透明。
- 保存后检查图片尺寸和透明通道，确保资源可直接用于游戏。

## C# 中文注释

- 后续新增或修改的 C# 代码须保持简短、准确的中文注释。
- 每个类、方法（含构造函数、Godot override 回调）和属性使用中文 XML `summary` 注释说明用途。
- 每个方法参数使用中文 XML `param` 注释，说明含义及适用的单位、方向约定、范围和默认值；有返回值时使用 `returns` 说明含义。
- 每个字段、常量添加简短中文注释；局部变量及循环变量也应简要说明其用途，可用紧邻声明的注释合并解释相关变量。
- 参数涉及时间、角度、位置、速度、图集索引或显示倍率时，明确单位和约定。
- 修改行为、单位、范围或默认值时同步更新注释，避免注释与代码不一致。
- 注释说明用途和必要约定，不逐行复述语法；不要求修改 Godot/.NET 自动生成文件或第三方代码。
- 仅补充注释时保留现有逻辑及用户调整的参数值，不引入额外功能。

## 项目结构文档

- 根目录 `Project-Structure.txt` 记录当前项目的目录树、C# 文件、类型及其描述；后续开发须同步维护。
- 新增、删除、重命名或移动目录、代码文件、类型，以及修改类型职责或 XML `summary` 注释时，在同一次修改中更新文档。
- 类型描述以源码 XML `summary` 为准，保留同文件多类型和嵌套类型；枚举、接口、结构体等注明类型，缺少描述时标记“缺少类型描述”，不自行推测。
- 所有收录目录均描述实际存放内容。资源只收录目录及其内容描述，不列具体资源文件，包括图片、字体、Godot 场景及 `.tres` 配置；资源目录的内容或用途改变时同步更新描述。
- 不列 `.uid`、`.import` 辅助文件；不收录 `.git/`、`.godot/`、`.tools/`、构建输出及本地临时目录。根目录保留项目配置和文档文件。
- 完成修改前核对目录与类型是否遗漏、移动后是否残留旧条目、描述是否与当前注释一致；仅记录已存在的结构，不将规划内容写成已实现。

# 项目协作约定

## 核心原则

- 当前 Codex Desktop 对话中的模型负责需求分析、设计、实现、测试和结果汇报，不使用子 Agent。
- 模型由用户在 Codex Desktop 当前对话中选择；项目配置不指定或切换模型。
- 本项目以战斗确定性和 Replay 可重现性为核心技术约束。
- 战斗逻辑固定 60Hz 推进；业务随机统一由 `VMath` 管理。
- 不得擅自改变用户已经确认的业务规则。
- 根据当前任务按需读取 `.codex/rules/` 中的领域规则，不预加载无关规则。
- 不因任务同时涉及设计、实现和测试而重复建立上下文；在当前对话内连续完成已授权的工作。

## 工作模式

### 讨论与设计

当用户明确处于讨论、分析或设计阶段时，只分析并形成方案，不提前修改代码。

不得为了实现方便改变已经确认的业务需求。分析涉及战斗确定性、Replay、Random、Timer、数学约定或其他既有领域约束时，必须遵守对应规则文件。

### 实现

用户明确授权新增、修改、重构或修复代码后，由当前模型直接完成生产代码修改。

实现过程中发现重要歧义，或必须改变已确认的核心设计、业务行为、数据结构或公共接口时，停止相关修改并向用户确认，不自行决定。

不得进行与当前任务无关的大规模重构，也不得为了顺手整理而扩大修改范围。

### 测试与验证

代码修改完成后，由当前模型直接执行必要的测试和验证，不另行委派测试 Agent。

默认采用最小相关测试（Targeted Testing），只测试本次修改直接影响的功能、类型和行为。共享基础设施修改可以根据实际依赖扩大为 Expanded Targeted Testing，但仍不自动执行完整回归测试。

“测试一下”“验证修改”“完成后测试”“检查是否正常”“确保没有问题”等普通要求均不代表完整回归测试。

只有用户明确要求“完整测试”“完整回归测试”“全部测试”“Full Regression”或具有同等明确含义的指示时，才执行完整回归测试。

不得仅根据模型自己的风险判断将 Targeted Testing 自行升级为 Full Regression。

## 领域规则加载

根据当前任务按需读取以下规则文件：

- 涉及战斗、Boss、Phase、Emitter、Bullet、随机、数学、时间、VTimer、Replay、方向约定或战斗服务：读取 `.codex/rules/battle.md`。
- 新增、修改、重构、审阅或测试 C# / Godot 代码：读取 `.codex/rules/coding.md`。
- 涉及 Boss 图片或相关美术资源：读取 `.codex/rules/assets.md`。

同一任务可以按需要读取多个规则文件，但不要读取与当前工作无关的规则。

纯讨论如果不涉及具体编码规范，不必读取 `coding.md`；纯 UI、文档或与战斗领域无关的任务不读取 `battle.md`。

## 连续任务原则

当用户在同一对话中先讨论方案、再授权实现、最后要求验证时，沿用当前对话已经确认的设计和上下文，不重新生成一套规格作为内部交接材料。

用户后续指令只改变其明确指定的部分；未被修改的既有约束继续有效。

完成工作后简要说明实际修改内容、执行过的测试及结果；没有执行完整回归测试时，不得表述为已经验证整个项目。
